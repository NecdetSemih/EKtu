<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giriş Sayfası</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Sayfa arka planı ve genel stil */
        body {
            background: linear-gradient(120deg, #a1c4fd 0%, #c2e9fb 100%);
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        .button-container {
            display: flex;
            gap: 30px;
            justify-content: center;
            align-items: center;
        }

        .btn-custom {
            padding: 20px 40px;
            font-size: 18px;
            border-radius: 12px;
            box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

            .btn-custom:hover {
                transform: translateY(-5px);
                box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.15);
            }

        .btn-student {
            background-color: #007bff;
            color: white;
        }

        .btn-teacher {
            background-color: #28a745;
            color: white;
        }
    </style>
</head>
<body>

    <div class="container">
        <div>
            <h1 class="mb-4">Bys Ktü Sistemine Hoşgeldiniz!</h1>
            <p class="lead">Lütfen giriş yapmak için aşağıdaki butonlardan birini seçin</p>
            <div class="button-container">
                <button class="btn btn-custom btn-student" onclick="window.location.href='Student/Login'">
                    Öğrenci Giriş
                </button>
                <button class="btn btn-custom btn-teacher" onclick="window.location.href='Instructor/Login'">
                    Öğretmen Giriş
                </button>
            </div>
        </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>

</body>
</html>
